import { FC } from 'react';

import Typography from '@mui/material/Typography';

const MCQView: FC = () => (
  <Typography color="text.secondary">Player coming in step 3.</Typography>
);

export default MCQView;
